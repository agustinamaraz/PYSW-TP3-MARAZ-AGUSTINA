import { TipoEspectadorPipe } from './tipo-espectador.pipe';

describe('TipoEspectadorPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoEspectadorPipe();
    expect(pipe).toBeTruthy();
  });
});
